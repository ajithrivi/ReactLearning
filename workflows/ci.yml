name: My CI/CD Pipeline

on:
  push:
    branches: [ main ]  # Runs when you push to 'main'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Get code from GitHub
      uses: actions/checkout@v3

    - name: Say Hello
      run: echo "✅ Code has been pushed and the CI robot is working!"

